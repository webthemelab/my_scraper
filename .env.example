# Copy this file to .env and fill in your real values.
# NEVER commit the actual .env file — it contains your passwords.

PGHOST=localhost
PGDATABASE=scrape_db
PGUSER=postgres
PGPASSWORD=your_strong_password_here
PGPORT=5432

# Use "require" for a remote server, "prefer" for local development
PGSSLMODE=prefer
